# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# EDA / 仿真产物
hq_run/
work/
transcript
vsim.wlf
*.wdb
*.mpf
modelsim.ini

# 大文件产物（权重与比特流不进 git，按需走 Release）
*.bit
export/weights/*.bin

# OS
Thumbs.db
Desktop.ini
.DS_Store

# M0 Python 工程补充（spec §3：采样产物不入库）
artifacts/
dist/

# F4 导出大文件不进 git（见 docs/quant-format.md；按需走 Release）
export/*.bin
